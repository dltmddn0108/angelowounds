# 상태관리

프론트엔드 상태관리는 규모가 커질수록 까다로워진다.

## 종류

- 로컬 상태: 컴포넌트 내부, 리액트 훅으로 충분
- 공유 상태: Context, Zustand, Redux
- 서버 상태: React Query, SWR

> [!note] 대부분의 앱은 Context로 충분하다.

리액트 훅을 사용할 때는 최소한의 상태만 유지하는 것이 좋다.
